{
  "name": "smm-ceo-application",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Share My Meals CEO application intake: magic-link, browser-recorded video, direct-to-S3 upload",
  "scripts": {
    "dev": "netlify dev",
    "create-bucket": "node scripts/create-bucket.js",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/s3-request-presigner": "^3.600.0",
    "@google/genai": "^0.7.0",
    "@sendgrid/mail": "^8.1.3"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
